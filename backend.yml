terraform {
  terraform_required_version = ">= 1.12.0"
  terraform_required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "5.99.1"
    }
  }
 
  backend "s3" {
    bucket = "new_uc-1_uc1"
    key    = "uc-01/terraform.tftstate"
    region = "us-east-1"
  }
}
